* on no link change during edit, dont send request
* pwd change works, but some inconsistent behaviour - to document
* if base-url changes and previous links active, should these still be deployed? or just hidden/cached?
* download url smart backslash at the end. should in-lay the 0.0.0.0:port if none set
* clean up make file, clean up scripts, clean up Dockerfile (ports and volumes set in docker file)
