/** Disable server-side rendering - the app is a fully client-side SPA */
export const ssr = false;
/** Disable static prerendering - all pages are dynamic and require a live API */
export const prerender = false;
